<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wbnull.hellobill.db.mapper.BillInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.wbnull.hellobill.db.entity.BillInfo">
        <id column="uuid" property="uuid" />
        <result column="username" property="username" />
        <result column="billTime" property="billtime" />
        <result column="topClass" property="topclass" />
        <result column="secondClass" property="secondclass" />
        <result column="detail" property="detail" />
        <result column="amount" property="amount" />
        <result column="remark" property="remark" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        uuid, username, billTime, topClass, secondClass, detail, amount, remark
    </sql>

</mapper>
